package com.mygdx.game;

public class Player {
	/* Player class for handling the update of each player each frame - use update() method
	 * Should have a draw() method for drawing the player with a specific color set in the constructor
	 */ 
}
